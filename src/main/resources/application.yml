spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/creative
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
server:
  error:
    include-message: always
jwt:
  secret: creative
  expiration: 14400